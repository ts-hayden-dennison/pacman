#! usr/bin/env python
import os
import pygame
class ImageLoader(object):
    '''def __init__(self, directory='.', imageType='.png', chunkSize=(16, 16)):
        self.directory = directory
        self.imageType = imageType
        self.images = []
        self.chunkSize = chunkSize
        self.load()
    def load(self):
        for img in os.listdir(self.directory):
            if img[-4:] == self.imageType:
                self.images.append(pygame.image.load(img).convert())
        self.chunk()
        return
    def chunk(self):
        return'''
